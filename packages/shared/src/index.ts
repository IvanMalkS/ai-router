export { scanRoutes, generateRoutesModule } from './scanner';
export type { ScannedRoute, PluginOptions } from './types';
export {
  VIRTUAL_MODULE_ID,
  RESOLVED_VIRTUAL_MODULE_ID,
  DEFAULT_EXTENSIONS,
  DEFAULT_EXCLUDE,
} from './types';
